# Arte de Magnus

Imágenes de marca usadas por la app (no borrar):

- `magnus-happy.svg` — beagle contento (login, resumen).
- `magnus-sleep.svg` — beagle tranquilo, ojos cerrados.
- `logo-horizontal.svg` — logotipo horizontal ("M" + "Magnus."), usado como wordmark.

Se referencian desde `src/components/magnus/magnusImages.ts`.
Si reemplazas un archivo, mantené el mismo nombre y fondo transparente.
